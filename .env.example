# Rime config used by scripts/preflight_check.js.
# Placeholders only — never commit real values.
RIME_MODEL_ID=coda
RIME_SPEAKER=astra
RIME_LANGUAGE=eng

# Set by Person 1 once the real Rime TTS integration needs it server-side.
# Never referenced from frontend code.
RIME_API_KEY=your_rime_api_key_here

# Backend stub tuning (optional)
TOOL_DELAY_MS=1500
RIME_DOWN=0
